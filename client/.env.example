# Base URL of the backend API server.
# Leave empty to use the Vite dev proxy (/api -> http://localhost:3001).
# Set to your deployed backend URL in production, e.g. https://api.yoursite.com
VITE_API_BASE_URL=http://localhost:3001
