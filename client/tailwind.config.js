/**
 * Tailwind CSS v4 is configured primarily through CSS (see src/index.css).
 * This file is optional in v4 and kept minimal for tooling that expects it.
 * Theme customization lives in the `@theme` block of index.css.
 */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
};
